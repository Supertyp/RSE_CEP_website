# Status: dark-mode

Complete. `@media (prefers-color-scheme: dark)` block in `global.css` overrides `--color-bg`, `--color-text`, `--color-muted`, `--color-border`, and `--color-accent` (brighter gold `#D4A017` for dark backgrounds). Dark mode verified on all five pages — background, text, and gold accent all correct.
