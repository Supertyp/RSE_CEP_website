# Status: drop-in sessions

Implementation complete and visually verified on the deployed GitHub Pages site. `src/pages/events.astro` renders at `/events/` with session meta, topic rotation table, format list, and Why section. "Events" nav link is active-highlighted on `/events/` and present in the mobile hamburger at ≤640 px viewport.

Next move: none — feature is stable and complete.
